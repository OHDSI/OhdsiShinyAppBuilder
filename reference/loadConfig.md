# loadConfig

Load the config specification

## Usage

``` r
loadConfig(configLocation)
```

## Arguments

- configLocation:

  The location of the config json file

## Value

An R list with the config settings

## Details

User specifies the config file and this function returns the R list with
the config

## See also

Other Config: [`addModuleConfig()`](addModuleConfig.md),
[`initializeModuleConfig()`](initializeModuleConfig.md),
[`saveConfig()`](saveConfig.md)
